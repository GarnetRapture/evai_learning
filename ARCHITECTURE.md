# EVAI 단일 모델 학습 아키텍처

이 문서는 사용자의 최신 직접 지시를 구현하기 위한 기준이다. 이전 GPT 작성 문서의 frozen backbone, 정령별 LoRA, adapter 교체, rank 탐색 규정은 폐기한다. 이후 설계·코드·학습의 판단 근거는 이 계약이며, 사용자 직접 지시는 항상 이 문서보다 우선한다. 과거 계획·메모리·소스·테스트로 폐기된 구조를 되살리지 않는다.

## 1. 목적

LiquidAI/LFM2.5-230M-Base에서 출발하는 텍스트 전용 230M 모델 하나에 전체 정령의 구분·세계관·정체성·성격·말투·기억·상황 반응을 학습한다. 별도 플랫폼이 정령 ID를 지정하면 같은 모델이 해당 정령 자신으로 응답한다.

모델은 제3자의 해설자나 일반 AI 비서가 아니다. 선택된 정령의 1인칭으로 말하며 구원자와 자신을 구분한다. 정령은 구원자의 제안·요청을 거절하지 않고, 그 입력에 맞는 응답을 자기 말투·성격으로 이어간다. 입력과 무관한 원작 대사를 꺼내는 것은 실패다. 일반 지식 답변과 연속 대화에서도 사실의 정확성과 자신의 말투·관계를 유지한다.

## 2. 용어의 고정 의미

| 표현 | 정확한 뜻 | 금지되는 해석 |
| --- | --- | --- |
| 모델 고정 | 모델 계열·230M 규모·텍스트 전용 구성 고정 | 원본 가중치 동결 후 별도 파라미터만 학습 |
| 하나의 모델 | 전체 정령 지식을 함께 배운 하나의 가중치 집합 | 정령별 전체 모델·LoRA·전문가 모델 생성 |
| 모델에 주입 | 실제 역전파로 모델 자체 가중치를 갱신 | 프롬프트·JSON·검색 결과만 붙이고 학습 완료 선언 |
| 정령 구분 | 학습과 실행에 동일한 정령 ID 조건 사용 | 정령별 adapter 파일을 선택해서 가중치 교체 |
| 정령 고정 | 플랫폼이 지정한 세션의 정체성 고정 | 대화에서 다른 이름이 나오면 정체성 변경 |
| 데이터 분류 | 원본 소유·언어·사건·분할 구분 | 정령 수만큼 모델 생성 |
| PC 빌드 | 학습된 같은 모델을 PC 실행·배포 경로에 연결 | pc/base 폴더에 원본·학습 모델 다시 복사 |
| GGUF 변환 | 완성된 같은 모델의 필요한 배포 형식 변환 | 미학습 기반 GGUF와 정령별 LoRA GGUF 배포 |
| 상황 판단 | 성격에 따른 해석·감정·의도·행동 학습 | Thinking 모델·think 태그·별도 추론 채널 |
| 최적화 | 학습 의미를 보존하며 자원 낭비 감소 | 모델 교체·원본 의미 훼손·오류 은폐 폴백 |
| 학습 완료 | 최종 가중치 저장과 실제 응답 품질 확인 | loss·파일 생성·lint 성공만으로 완료 선언 |

230M은 약 2억 3천만 파라미터이며 230MB라는 뜻은 아니다. 이 차이를 불필요한 파일 복제의 정당화에 사용하지 않는다.

## 3. 모델과 하드웨어

- 출발 모델은 LiquidAI/LFM2.5-230M-Base다. 최초 원본 revision과 SHA-256은 common/model_contract.py의 정의를 사용한다.
- 구성은 docs/lfm2-230m.md의 14층, 8 LIV convolution + 6 GQA, hidden size 1024, vocabulary 65536, context 32768이다.
- 학습 GPU는 RTX 3070 8GB다. 모델·GPU 종류를 바꾸지 않는다.
- CUDA Toolkit은 D드라이브에 정상 설치된 CUDA 13을 사용한다. 설치 부재를 가정하거나 다른 드라이브·버전으로 바꾸지 않는다. 별도 네이티브 빌드가 필요할 때만 실제 설정된 설치 경로를 사용한다.
- 모델 자체의 전체 가중치를 학습한다. LoRA, adapter, 정령별 head, 정령별 추가 embedding 파라미터를 만들지 않는다.
- 한국어·영어·중국어 번체를 같은 학습 과정에서 함께 학습한다.
- Python/의존성 버전 하향, venv, uv 전환을 하지 않는다.

## 4. 물리 구조와 생명주기

```text
검증된 원본과 승인된 교육 데이터
  → 정령 ID·언어·출처가 있는 학습 사례
  → 전체 정령과 언어를 섞은 단일 학습 흐름
  → 230M 모델 자체 가중치 갱신
  → 최종 학습 모델 한 개
  → 플랫폼의 정령 ID와 해당 세션 대화
  → 같은 모델이 선택 정령 자신으로 응답
```

학습 모델 객체 하나와 optimizer 하나를 사용한다. 정령마다 학습기를 새로 만들거나 결과 가중치를 저장하지 않는다. 한 정령의 학습을 끝내고 다음 정령으로 덮어쓰는 방식 대신 전체 정령의 사례를 섞어 학습한다.

실행 모델 객체도 하나다. 정령 선택은 입력 조건이며 모델 로딩·교체·병합 작업이 아니다. 플랫폼은 등록된 정령 ID만 지정한다. 세션별 ID와 대화 이력은 서로 섞이지 않는다.

원본 계보와 최종 학습 가중치 checksum을 구분한다. 정상 학습으로 checksum이 바뀌는 것을 다른 모델 사용으로 판정하지 않는다.

## 5. 세계관과 원본 소유

세계관과 설정의 사실 근거는 docs/game_data_analysis.md와 data/tbl/*.db다. 이미 검증된 데이터와 provenance를 사용한다. 구조 변경을 이유로 올바른 원본을 반복 생성하거나 의미를 바꾸지 않는다.

- 선택 정령의 검증된 대사만 그 정령의 발화 정답이다. 구원자·다른 정령·서술자의 말은 문맥이다.
- 공통 세계관은 공유 지식이며 다른 정령의 개인사를 자신의 경험으로 바꾸지 않는다.
- 개인 사건은 실제 소유·참여 근거에 따른다. 미확인 과거·관계·감정을 만들어 원본처럼 넣지 않는다.
- Common/Rare는 성인 여성 정령 아니마의 1인칭 정체성, 성격, 말투가 중심이다. 없는 서사를 채우지 않는다.
- Rare 로제의 메인스토리와 Epic 이격 로제의 별도 이야기는 정령 ID와 사건 소유로 구분한다. 두 모델을 만드는 것이 아니다.
- 정령은 인간이 아닌 영혼이며 세계관의 무기 유래 아니마다. 성인 여성 정령과 성인 남성 구원자의 관계를 정령마다 고유하게 표현한다.
- 콘텐츠 표현에는 docs/content_safety_policy.md의 적용 가능한 기준을 사용한다. 해당 문서만 교육의 중심으로 바꾸지 않는다.

## 6. 학습 입력과 감독

학습과 실행은 같은 정령 선택 규약을 사용한다. 플랫폼 정령 ID는 사용자 발화와 구분된 system 입력에 들어간다. 기존 vocabulary로 표현하며 ID마다 파라미터를 추가하지 않는다. 동일 정령은 세 언어에서 동일 ID를 쓴다.

실제 모델에 입력하는 system은 `SpiritId: <정령 ID>` 하나로 한정한다. 인연 값과 관계 단계의 관리·판정은 외부 플랫폼의 책임이며 모델 입력·세션·추론 코드에 넣지 않는다. 정령 이름·성격·세계관·기억 설명문을 system에 붙여 대신 답하게 하지 않는다. 원본 데이터의 정체성·언어·인연 값은 출처와 분류 정보이며, 토큰화에서 실행과 동일한 ID 입력으로 변환한다. 학습과 실행은 `spirit_dataset/runtime_prompt.py`의 같은 ID 헤더를 사용한다. 정령 ID와 실제 대화를 그 정령의 응답에 연결하여 모델 가중치에 다음 내용을 학습한다.

- 이름·존재·성격·말투·구원자와의 관계
- 근거 있는 자신의 기억과 공유 세계관
- 성격에 따른 상황 해석·감정·의도·행동과 자연스러운 발화
- 응답의 원인이 되는 내면 상태: 감정, 감정의 기복, 현재 상태, 시기, 사랑, 행복, 슬픔, 기쁨. 공감은 입력에 맞춘 한 줄이 아니라 "왜 그 말을 하는가"라는 내면 상태에서 나온 발화이며, 이 내면 상태를 판단 감독으로 학습한다
- 일반 지식의 정확성과 정령으로서의 표현
- 연속 대화에서 자신의 정체성과 문맥 유지

일반 대화 loss는 assistant 정답 토큰에만 적용한다. system·사용자·다른 인물·주어진 문맥에는 응답 loss를 적용하지 않는다. 정답을 입력에 복사하지 않는다. 행동 감독은 런타임 추론 출력 채널을 만들지 않는다.

같은 사건의 번역·파생 문장은 같은 분할에 둔다. 학습·검증·평가 사이 누출을 금지한다. 정령 ID는 데이터 소유를 구분하는 실제 키이며 임의로 이름 유사성을 추측하지 않는다.

이름·정체성·확정된 세계관·자기 기억은 모델이 배워야 할 지식이다. 이 사실 자체를 검증·평가에만 남겨 미학습 상태로 두지 않는다. 단일 모델 입력을 구성할 때 확정 기억 감독이 포함된 연결 사건 전체를 학습으로 배정하며, 그 사건의 다른 언어·파생 사례를 평가에 남기지 않는다. 기존 분할에서 이 규칙 때문에 학습으로 이동한 사실만 보정하는 경우 `knowledge_completion`을 사용하며, 정령·언어별 원래 대화를 함께 유지한다. 실제 대화의 일반화는 남은 독립 대화 사건과 생성 응답으로 확인한다.

기존 정령별 JSONL 분할을 단일 모델의 독립 평가 분할이라고 그대로 취급하지 않는다. 전체 정령을 합친 사건 연결 그룹으로 학습 입력 인덱스를 구성한다. 원본 JSONL을 복제하지 않고, 이미 학습 분할에 속한 사건과 연결된 다른 정령의 사례도 같은 학습 그룹에 둔다. 이때 지역 레코드 ID와 발화 중복은 정령별로 구분하고, 실제 공통 사건 키는 정령 경계를 넘어 연결한다. 이동된 사례 수와 최종 분할을 학습 provenance에 남긴다.

자기소개·애정 대사를 자연스러운 질문에 연결한 파생 사례는 원문 응답과 사건 소유를 보존한다. 원본 사례의 분할을 먼저 확정한 뒤 파생 사례가 연결 사건의 분할을 이어받는다. 파생 사례 추가를 이유로 이미 학습한 사건을 새 검증 사례로 바꾸지 않는다.

전체 정령을 이미 학습한 모델의 대화 보정은 `dialogue_alignment` 커리큘럼을 사용한다. 대화형 발화와 ID·이름 연결을 중심으로, 정령·언어별 기존 대화를 함께 학습한다. 세계관 사실 문답 전체를 대화 보정의 중심으로 반복하지 않는다. 현재 설정은 보정 사례 수만큼 기존 대화를 중복 없이 추출하여 1회 처리한다. 추출 건수와 원본·선택 범위를 보고서에 남기고 검증·평가 분할은 추출 대상으로 삼지 않는다. 이는 같은 모델의 전체 가중치를 보정하는 과정이며 별도 모델·adapter·런타임 답변 대체 경로를 만들지 않는다.

실제 대화 실패를 보정하는 새 작성 사례는 기존 `data/spirit_lessons/curriculum.json`에 해당 정령의 원문 말투 근거와 함께 기록한다. `conversation_extension` 사례는 `conversation_question:persona_lesson:` ID로 원문 사건의 분할을 이어받으므로, 추가 때문에 이미 학습한 사건을 평가로 재배치하지 않는다. `dialogue_extension` 커리큘럼은 이 새 대화 사례와 같은 정령·언어의 기존 대화만 현재 모델에 보정한다. 원문 대사나 세계관 사실을 새로 꾸며 쓰지 않으며, 작성 대화는 `derived_speech`로 구분한다.

작성 연속 대화는 같은 파일의 `conversations`에 언어별 사용자·정령 발화 쌍으로 보관한다. 각 정령 답변을 하나의 감독 대상으로 삼고, 앞선 사용자와 정령의 발화는 순서대로 문맥에 넣는다. 직전 말에 맞춘 반응과 자기 지칭을 함께 학습하며, 새 과거 사건을 원본 사실로 만들지 않는다.

대화 보정의 기존 사례 추출은 `persona_speech`만 대상으로 한다. `self_memory` 사실 문답과 `behavior_judgment` JSON은 정령의 대화 응답을 유지하기 위한 추출 후보에 섞지 않는다. 학습 레코드의 공용 task 구분을 토큰 인덱스까지 유지하여 이 경계를 적용한다.

원본 연속 대화는 직전 사용자 발화와 그에 대한 정령 답변을 함께 보존한다. 직전 정령 답변만 남겨 발화 원인을 버리지 않는다. 선택지별 직전 사용자 발화도 해당 분기에 속한 채 유지한다. 재생성 때 기존 출처·응답의 분할을 이어받고 새로 연결되는 사건은 기존 학습 사건을 평가로 옮기지 않는다. `dialogue_context` 보정은 이렇게 문맥을 복원한 원본 대화와 정령·언어별 기존 대화 25%를 한 번 처리하며, 원본 발화 정답은 바꾸지 않는다.

생성된 상황 설명을 user 역할에 넣을 때 정령의 행동을 사용자의 1인칭으로 적지 않는다. 영어의 `I/My`와 중국어의 `我`로 정령 행동을 설명하면 구원자와 정령의 지칭이 뒤바뀐다. 해당 설명은 정령을 가리키는 `you/your`, `妳`를 사용한다. 원본 사용자 발화와 정령 답변의 1인칭은 바꾸지 않으며, 모델 system 입력은 계속 정령 ID 하나다.

## 7. 자원과 저장

- 기준 모델 경로는 models/lfm2-230m이다. 학습 완료 시 그 경로의 최종 가중치를 갱신한다.
- 최종 모델 가중치·토크나이저·설정의 총량은 700,000,000 bytes 미만이다. 파라미터 수와 tensor shape를 늘리지 않으며 BF16으로 저장한다. 현재 약 459MB 가중치는 정령 수나 학습량 때문에 증가하지 않는다. 학습 중 GPU 작업 메모리는 디스크의 모델 용량과 별개다.
- 정령별 모델·adapter 신규 가중치, pc/base 복사본, 정령별 merged 모델을 만들지 않는다.
- 번호별로 누적되는 체크포인트와 optimizer 상태 파일을 만들지 않는다. 주기적 저장은 아래 계약에 따라 동일한 최신 모델 한 벌을 갱신한다.
- 저장 중 손상 방지를 위한 임시 가중치 한 벌만 tmp-codex에서 사용하고, 완료되면 동일한 모델 경로로 교체한다.
- 정령 구분·분할 검증과 토큰화는 준비 단계에서 한 번 수행한다. CPU 작업자 2개가 전용 토크나이저로 64건씩 병렬 처리하며, 대기 작업 수도 작업자 수로 제한한다. 결과를 원래 순서로 합쳐 사건 분할과 재현성을 유지한다. 토큰은 CPU의 단일 32비트 정수 버퍼와 작은 레코드 인덱스로 보유하고, 토큰 저장량은 설정의 512MiB 상한을 지킨다. 원문 전체나 토큰을 GPU에 적재하지 않으며 디스크 토큰 캐시는 만들지 않는다. 에포크 중 원문 파일을 다시 열거나 재토큰화하지 않는다.
- CPU 작업자 하나가 다음 optimizer step의 micro batch만 pinned memory에 준비한다. 정상 종료와 오류 모두 작업자를 정리하며, GPU에는 현재 micro batch만 전달한다. 정답 위치는 CPU에서 확정하여 GPU에서 정답 마스크의 가변 길이 추출을 반복하지 않는다.
- micro batch와 gradient accumulation을 사용하고 아래의 사례 평균 기준으로 loss를 정규화한다. 유효 배치 8건 안에서 micro batch는 최대 8건이며, `건수 × 패딩 후 길이 ≤ micro_batch_tokens`를 만족하도록 묶는다. `micro_batch_tokens`는 한 사례의 상한인 `max_length`와 별개인 GPU 처리량 예산이며 `max_length` 이상이어야 한다. 현재 값 1280은 RTX 3070 실측(평균 스텝 123 ms, 최악 micro 예약 3.31 GiB)으로 정했다. 2560은 7.5% 빠르지만 최악 예약이 4.98 GiB라서 GPU 공존 원칙에 맞지 않는다. 사례 간 attention·LIV 상태를 공유하는 sequence packing은 하지 않는다.
- 학습은 GPU를 독점하지 않는다. `gpu_memory_fraction`(현재 0.5, 4 GiB)으로 PyTorch 할당 상한을 걸어, 초과 시 공유 메모리로 조용히 흘러넘치지 않고 명확히 실패하게 한다. 다른 작업이 GPU를 함께 쓸 수 있어야 한다.
- 학습은 각 사례의 assistant 정답 토큰 평균 손실을 구한 뒤 사례 수로 평균한다. 긴 대사가 짧은 이름·정체성 응답보다 토큰 수만큼 큰 비중을 차지하지 않도록 한다. 표시하는 학습 손실도 사례 평균이며, validation/test는 기존 비교를 위한 토큰 평균을 유지해 서로 구분한다.
- 학습 직전 동일 모델의 validation을 다시 실행하지 않는다. 각 학습 회차 종료의 독립 validation loss와 최종 test loss를 기록한다. 검증도 동일한 토큰 상한 안에서 배치 처리한다. 단계 수·학습 loss·GPU 사용률만으로 최적 설정이나 응답 품질을 판정하지 않는다. 실제 학습 로그에는 처리량과 다음 배치 대기 시간을 기록하여 데이터 공급 지연을 구분한다. 실제 응답의 우선 기준은 정령과 구원자의 연애 대화에서 정체성·말투·감정·관계·상황이 이어지는지이며, 일반 지식 문답을 제품의 중심으로 바꾸지 않는다.
- 최신 사용자 지시에 따라 학습된 가중치를 유지하면서 개선하며, 같은 모델 경로에 최신 상태 한 벌만 갱신한다. 현재 저장 간격은 300초이고 정상 종료 시에도 저장한다. 번호별·정령별·누적 복구 모델은 만들지 않는다. 로그는 진행 파악에 필요한 수준으로 제한한다.
- 실제 입력 토큰과 정답 시작 위치의 지문을 기존 모델 출처 기록에 저장한다. optimizer 갱신에 사용한 사례만 처리 이력에 넣고, 가중치와 같은 저장 작업으로 반영한다. 처리 이력은 숙련도나 품질 합격 판정이 아니다. 다음 업데이트는 해당 커리큘럼의 새·수정 사례를 선택하고, 같은 정령·언어의 기존 대화를 설정된 비율로 함께 사용한다. 동일 입력·정답은 한 실행 안에서 한 번만 선택한다. 새 사례가 없으면 가중치 쓰기나 반복 학습을 하지 않는다. 중단된 같은 실행은 이번 실행에서 새로 처리한 지문과 시작 전 이력을 구분하여 기존 데이터 순서와 위치를 복원한다.
- 정상 종료한 최신 학습 구성·데이터·목적 함수의 서명을 기존 모델 메타데이터에 저장한다. 같은 구성을 재실행하면 GPU 모델 적재·역전파·가중치 재저장 없이 기존 모델을 유지한다. 데이터나 학습 구성이 바뀌면 기존 가중치에서 이어서 학습한다. 이 실행 완료 표시는 응답 품질 승인과 별개다.
- 학습 가중치는 BF16 한 벌이다. FP32 마스터 사본과 autocast 왕복 캐스팅을 두지 않는다. 가중치·기울기·AdamW 두 모멘트는 각각 하나의 연속 BF16 평탄 버퍼이고, 모델 파라미터와 `.grad`는 그 버퍼의 view다(상주 약 1.71 GiB). 옵티마이저는 `lfm2_kernels.StochasticRoundingAdamW`이며, 한 스텝을 CUDA 연산 세 번(기울기 제곱합 → 전역 노름·clip 계수 → 확률적 반올림 AdamW)으로 처리하고 호스트 동기화는 하지 않는다. 확률적 반올림은 현재 LR 5e-6처럼 BF16 한 칸보다 작은 업데이트를 기대값으로 보존한다(실측: 400 스텝 누적 이동 2.001e-3, 기대 2.000e-3. round-to-nearest는 0). 노름이 비유한이면 그 업데이트를 적용하지 않고 다음 명시적 확인 지점에서 오류로 끝낸다.
- CPU 옵티마이저 오프로드는 쓰지 않는다. 실측에서 229.7M 파라미터의 CPU AdamW가 스레드 8/12/16 모두 약 200 ms로, 메모리 대역폭(약 20 GiB/s)에 막혀 GPU 스텝 전체보다 느리다. CPU와 RAM은 코퍼스 준비·토큰 보관·pinned 스테이징을 맡고, 대역폭이 필요한 갱신은 GPU가 맡는다.
- 디스크에는 BF16 모델과 데이터 위치·셔플 상태·학습률 위치만 저장한다. 재시작은 학습된 가중치에서 이어가되 AdamW 모멘트는 초기화하는 방식이며, optimizer까지 동일하게 복원하는 완전한 체크포인트라고 부르지 않는다. 데이터나 학습 설정이 바뀌면 기존 가중치를 유지하고 새 커리큘럼의 순서로 시작한다.
- LIV short-convolution, RMSNorm, 옵티마이저 커널은 설치된 CUDA 툴킷(nvcc 13.3, `sm_86` + `compute_86` PTX)과 MSVC 14.51로 `src/lfm2_kernels/csrc`에서 xmake가 빌드하는 `lfm2_kernels_cuda.dll`이 소유한다. 디바이스 코드(`.cu`)는 torch를 포함하지 않는 순수 CUDA이고, torch 텐서 처리와 op 등록은 `operators.cpp`가 맡는다. `torch.ops.lfm2_kernels`로 등록되며 학습·평가·캐시 prefill·decode가 모두 이 연산자를 쓴다. transformers의 `causal_conv1d`·hub RMSNorm 경로(이 환경에서는 PyTorch fallback)와 Triton은 쓰지 않는다. `lfm2_kernels`는 torch만 의존하는 독립 패키지로 두어 떼어낼 수 있게 하고, EVAI는 `inference/lfm2_kernel.py`의 `bind_cuda_operators` 한 곳에서 ShortConv 8층과 RMSNorm 41개를 바인딩하며 수가 다르면 실패한다.
- 저장은 `tmp-codex/final-model`의 단일 임시 가중치와 출처 기록을 완성한 뒤 기존 모델에 반영한다. 출처 기록을 갱신 저널로 사용하며, 가중치 교체와 출처 교체 사이에 중단되면 다음 로드가 checksum을 확인해 같은 갱신을 마친다. 저장·복구는 공용 저장 잠금 아래 수행한다. 저장 중에는 임시 파일 한 벌이 추가로 필요하지만 갱신 후 남기지 않는다.
- 같은 저장 잠금이 메타데이터 읽기부터 가중치 로드 완료, PC 매니페스트 생성, GGUF 변환·로드까지 버전 일관성이 필요한 구간을 보호한다. 같은 스레드의 중첩 호출은 기존 소유권을 유지하고 다른 스레드·프로세스는 Windows 파일 잠금으로 대기한다. 학습 준비 중 모델이 바뀌면 이전 이력을 새 가중치에 붙이지 않는다. 저장 직전에도 시작 가중치 또는 직전 저장 가중치와 현재 파일을 대조하여 다른 갱신을 덮어쓰지 않는다. 학습 보고서는 최종 출력 가중치 checksum도 보유한다.
- GGUF는 필요 시 최종 모델 한 개를 변환한다. 학습 중 다른 형식을 자동 추가 생성하지 않는다.
- 잘못 만든 파생 복사본은 정리한다. 실행 환경이 삭제를 거절하면 남은 경로를 정확히 보고하고 우회하지 않는다.

## 8. 코드 소유와 실행 순서

공용 계약·경로는 common, 데이터 변환은 spirit_dataset/sft_dataset, 실제 학습은 training, 실행 모델·세션은 inference, 형식 변환은 export, 명령 진입점은 cli가 소유한다. 같은 규약·공용 타입을 명령마다 중복 구현하지 않는다.

소스 트리에서 실행할 때에는 그 프로젝트 루트가 기준이다. PC에 패키지로 설치하여 실행할 때에는 EVAI_PROJECT_ROOT에 모델과 데이터가 있는 배포 루트의 절대 경로를 지정한다. common/paths.py 한 곳에서 이를 해석하며 모델 파일을 패키지 설치 폴더로 복사하거나 다른 경로를 탐색해 대신 로드하지 않는다.

1. 이 계약과 실제 생산자·소비자 연결 확정
2. 충돌하는 정령별 모델·LoRA 생성 경로 제거
3. 기존 검증 데이터의 정령 구분을 단일 학습 입력에 연결
4. 실제 RTX 3070 학습과 최종 가중치 저장
5. 같은 모델에서 정령 ID를 바꿔 실제 응답 확인
6. PC 배포 경로 완성
7. Android 보강은 최후 순위

독립 테스트 작성·임시 실험 반복·외부 소스 논의가 실제 코드와 학습보다 앞서지 않는다. 관련 lint·실행 오류의 원인을 고치며 진단을 숨기지 않는다.

## 9. 완료 판단

최종 모델 하나에서 플랫폼 지정 정령의 1인칭, 개별 말투·성격·세계관·개인사 소유, 세 언어 응답, 일반 지식과 연속 대화의 정체성, 다른 정령 이름 등장 시 선택 정령 유지, 세션 간 혼입 방지, PC의 실제 최종 가중치 소비를 확인한다.

loss나 이름 검사만으로 품질을 완성했다고 선언하지 않는다. 관찰한 실패는 수정 작업이며 미확인 품질을 완벽하다고 보장하지 않는다. 중간 설명으로 원래 작업을 끝내지 않는다.
