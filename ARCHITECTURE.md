# EVAI 단일 모델 학습 아키텍처

이 문서는 사용자의 최신 직접 지시를 구현하기 위한 기준이다. 이전 GPT 작성 문서의 frozen backbone, 정령별 LoRA, adapter 교체, rank 탐색 규정은 폐기한다. 이후 설계·코드·학습의 판단 근거는 이 계약이며, 사용자 직접 지시는 항상 이 문서보다 우선한다. 과거 계획·메모리·소스·테스트로 폐기된 구조를 되살리지 않는다.

## 1. 목적

LiquidAI/LFM2.5-230M-Base에서 출발하는 텍스트 전용 230M 모델 하나에 전체 정령의 구분·세계관·정체성·성격·말투·기억·상황 반응을 학습한다. 별도 플랫폼이 정령 ID를 지정하면 같은 모델이 해당 정령 자신으로 응답한다.

모델은 제3자의 해설자나 일반 AI 비서가 아니다. 선택된 정령의 1인칭으로 말하며 구원자와 자신을 구분한다. 일반 지식 답변과 연속 대화에서도 사실의 정확성과 자신의 말투·관계를 유지한다.

## 2. 용어의 고정 의미

| 표현 | 정확한 뜻 | 금지되는 해석 |
| --- | --- | --- |
| 모델 고정 | 모델 계열·230M 규모·텍스트 전용 구성 고정 | 원본 가중치 동결 후 별도 파라미터만 학습 |
| 하나의 모델 | 전체 정령 지식을 함께 배운 하나의 가중치 집합 | 정령별 전체 모델·LoRA·전문가 모델 생성 |
| 모델에 주입 | 실제 역전파로 모델 자체 가중치를 갱신 | 프롬프트·JSON·검색 결과만 붙이고 학습 완료 선언 |
| 정령 구분 | 학습과 실행에 동일한 정령 ID 조건 사용 | 정령별 adapter 파일을 선택해서 가중치 교체 |
| 정령 고정 | 플랫폼이 지정한 세션의 정체성 고정 | 대화에서 다른 이름이 나오면 정체성 변경 |
| 데이터 분류 | 원본 소유·언어·사건·분할 구분 | 정령 수만큼 모델 생성 |
| PC 빌드 | 학습된 같은 모델을 PC 실행·배포 경로에 연결 | pc/base 폴더에 원본·학습 모델 다시 복사 |
| GGUF 변환 | 완성된 같은 모델의 필요한 배포 형식 변환 | 미학습 기반 GGUF와 정령별 LoRA GGUF 배포 |
| 상황 판단 | 성격에 따른 해석·감정·의도·행동 학습 | Thinking 모델·think 태그·별도 추론 채널 |
| 최적화 | 학습 의미를 보존하며 자원 낭비 감소 | 모델 교체·원본 의미 훼손·오류 은폐 폴백 |
| 학습 완료 | 최종 가중치 저장과 실제 응답 품질 확인 | loss·파일 생성·lint 성공만으로 완료 선언 |

230M은 약 2억 3천만 파라미터이며 230MB라는 뜻은 아니다. 이 차이를 불필요한 파일 복제의 정당화에 사용하지 않는다.

## 3. 모델과 하드웨어

- 출발 모델은 LiquidAI/LFM2.5-230M-Base다. 최초 원본 revision과 SHA-256은 common/model_contract.py의 정의를 사용한다.
- 구성은 docs/lfm2-230m.md의 14층, 8 LIV convolution + 6 GQA, hidden size 1024, vocabulary 65536, context 32768이다.
- 학습 GPU는 RTX 3070 8GB다. 모델·GPU 종류를 바꾸지 않는다.
- CUDA Toolkit은 D드라이브에 정상 설치된 CUDA 13을 사용한다. 설치 부재를 가정하거나 다른 드라이브·버전으로 바꾸지 않는다. 별도 네이티브 빌드가 필요할 때만 실제 설정된 설치 경로를 사용한다.
- 모델 자체의 전체 가중치를 학습한다. LoRA, adapter, 정령별 head, 정령별 추가 embedding 파라미터를 만들지 않는다.
- 한국어·영어·중국어 번체를 같은 학습 과정에서 함께 학습한다.
- Python/의존성 버전 하향, venv, uv 전환을 하지 않는다.

## 4. 물리 구조와 생명주기

```text
검증된 원본과 승인된 교육 데이터
  → 정령 ID·언어·출처가 있는 학습 사례
  → 전체 정령과 언어를 섞은 단일 학습 흐름
  → 230M 모델 자체 가중치 갱신
  → 최종 학습 모델 한 개
  → 플랫폼의 정령 ID와 해당 세션 대화
  → 같은 모델이 선택 정령 자신으로 응답
```

학습 모델 객체 하나와 optimizer 하나를 사용한다. 정령마다 학습기를 새로 만들거나 결과 가중치를 저장하지 않는다. 한 정령의 학습을 끝내고 다음 정령으로 덮어쓰는 방식 대신 전체 정령의 사례를 섞어 학습한다.

실행 모델 객체도 하나다. 정령 선택은 입력 조건이며 모델 로딩·교체·병합 작업이 아니다. 플랫폼은 등록된 정령 ID만 지정한다. 세션별 ID와 대화 이력은 서로 섞이지 않는다.

원본 계보와 최종 학습 가중치 checksum을 구분한다. 정상 학습으로 checksum이 바뀌는 것을 다른 모델 사용으로 판정하지 않는다.

## 5. 세계관과 원본 소유

세계관과 설정의 사실 근거는 docs/game_data_analysis.md와 data/tbl/*.db다. 이미 검증된 데이터와 provenance를 사용한다. 구조 변경을 이유로 올바른 원본을 반복 생성하거나 의미를 바꾸지 않는다.

- 선택 정령의 검증된 대사만 그 정령의 발화 정답이다. 구원자·다른 정령·서술자의 말은 문맥이다.
- 공통 세계관은 공유 지식이며 다른 정령의 개인사를 자신의 경험으로 바꾸지 않는다.
- 개인 사건은 실제 소유·참여 근거에 따른다. 미확인 과거·관계·감정을 만들어 원본처럼 넣지 않는다.
- Common/Rare는 성인 여성 정령 아니마의 1인칭 정체성, 성격, 말투가 중심이다. 없는 서사를 채우지 않는다.
- Rare 로제의 메인스토리와 Epic 이격 로제의 별도 이야기는 정령 ID와 사건 소유로 구분한다. 두 모델을 만드는 것이 아니다.
- 정령은 인간이 아닌 영혼이며 세계관의 무기 유래 아니마다. 성인 여성 정령과 성인 남성 구원자의 관계를 정령마다 고유하게 표현한다.
- 콘텐츠 표현에는 docs/content_safety_policy.md의 적용 가능한 기준을 사용한다. 해당 문서만 교육의 중심으로 바꾸지 않는다.

## 6. 학습 입력과 감독

학습과 실행은 같은 정령 선택 규약을 사용한다. 플랫폼 정령 ID는 사용자 발화와 구분된 system 입력에 들어간다. 기존 vocabulary로 표현하며 ID마다 파라미터를 추가하지 않는다. 동일 정령은 세 언어에서 동일 ID를 쓴다.

사례는 정령 ID, 언어, 현재 대화, 필요한 짧은 정체성 문맥과 그 정령의 응답을 연결한다. 모델 가중치에 다음 내용을 학습한다.

- 이름·존재·성격·말투·구원자와의 관계
- 근거 있는 자신의 기억과 공유 세계관
- 성격에 따른 상황 해석·감정·의도·행동과 자연스러운 발화
- 일반 지식의 정확성과 정령으로서의 표현
- 연속 대화에서 자신의 정체성과 문맥 유지

일반 대화 loss는 assistant 정답 토큰에만 적용한다. system·사용자·다른 인물·주어진 문맥에는 응답 loss를 적용하지 않는다. 정답을 입력에 복사하지 않는다. 행동 감독은 런타임 추론 출력 채널을 만들지 않는다.

같은 사건의 번역·파생 문장은 같은 분할에 둔다. 학습·검증·평가 사이 누출을 금지한다. 정령 ID는 데이터 소유를 구분하는 실제 키이며 임의로 이름 유사성을 추측하지 않는다.

기존 정령별 JSONL 분할을 단일 모델의 독립 평가 분할이라고 그대로 취급하지 않는다. 전체 정령을 합친 사건 연결 그룹으로 학습 입력 인덱스를 구성한다. 원본 JSONL을 복제하지 않고, 이미 학습 분할에 속한 사건과 연결된 다른 정령의 사례도 같은 학습 그룹에 둔다. 이때 지역 레코드 ID와 발화 중복은 정령별로 구분하고, 실제 공통 사건 키는 정령 경계를 넘어 연결한다. 이동된 사례 수와 최종 분할을 학습 provenance에 남긴다.

## 7. 자원과 저장

- 기준 모델 경로는 models/lfm2-230m이다. 학습 완료 시 그 경로의 최종 가중치를 갱신한다.
- 최종 모델 가중치·토크나이저·설정의 총량은 700,000,000 bytes 미만이다. 파라미터 수와 tensor shape를 늘리지 않으며 BF16으로 저장한다. 현재 약 459MB 가중치는 정령 수나 학습량 때문에 증가하지 않는다. 학습 중 GPU 작업 메모리는 디스크의 모델 용량과 별개다.
- 정령별 모델·adapter 신규 가중치, pc/base 복사본, 정령별 merged 모델을 만들지 않는다.
- 누적·순환·주기적 체크포인트와 optimizer 상태 파일을 만들지 않는다.
- 최종 저장 중 손상 방지를 위한 임시 파일 하나만 tmp-codex에서 사용하고 최종 경로로 교체한다. 중간 학습 복구점이 아니다.
- 정령 구분·분할 검증과 토큰화는 준비 단계에서 한 번 수행한다. 토큰은 CPU의 단일 32비트 정수 버퍼와 작은 레코드 인덱스로 보유하고, 토큰 저장량은 설정의 512MiB 상한을 지킨다. 원문 전체나 토큰을 GPU에 적재하지 않으며 디스크 토큰 캐시는 만들지 않는다. 에포크 중 원문 파일을 다시 열거나 재토큰화하지 않는다.
- CPU 작업자 하나가 다음 optimizer step의 micro batch만 pinned memory에 준비한다. 정상 종료와 오류 모두 작업자를 정리하며, GPU에는 현재 micro batch만 전달한다. 정답 위치는 CPU에서 확정하여 GPU에서 정답 마스크의 가변 길이 추출을 반복하지 않는다.
- micro batch와 gradient accumulation을 사용하고 유효 정답 토큰 수로 loss를 정규화한다. 유효 배치 8건 안에서 micro batch는 최대 8건이며, `건수 × 패딩 후 길이 ≤ max_length(현재 1280)`를 만족하도록 묶는다. 짧은 사례를 불필요하게 1건씩 실행하지 않고, 긴 사례는 같은 토큰 상한 안에서 작은 묶음으로 처리한다. 사례 간 attention·LIV 상태를 공유하는 sequence packing은 하지 않는다.
- 각 에포크에서 독립 validation loss를 기록하고, 마지막에 test loss와 실제 응답을 확인한다. 단계 수·학습 loss·GPU 사용률만으로 최적 설정이나 응답 품질을 판정하지 않는다. 실제 학습 로그에는 처리량과 다음 배치 대기 시간을 기록하여 데이터 공급 지연을 구분한다.
- 가중치는 학습 종료 시 한 번 저장한다. 로그는 진행 파악에 필요한 수준으로 제한한다.
- GGUF는 필요 시 최종 모델 한 개를 변환한다. 학습 중 다른 형식을 자동 추가 생성하지 않는다.
- 잘못 만든 파생 복사본은 정리한다. 실행 환경이 삭제를 거절하면 남은 경로를 정확히 보고하고 우회하지 않는다.

## 8. 코드 소유와 실행 순서

공용 계약·경로는 common, 데이터 변환은 spirit_dataset/sft_dataset, 실제 학습은 training, 실행 모델·세션은 inference, 형식 변환은 export, 명령 진입점은 cli가 소유한다. 같은 규약·공용 타입을 명령마다 중복 구현하지 않는다.

소스 트리에서 실행할 때에는 그 프로젝트 루트가 기준이다. PC에 패키지로 설치하여 실행할 때에는 EVAI_PROJECT_ROOT에 모델과 데이터가 있는 배포 루트의 절대 경로를 지정한다. common/paths.py 한 곳에서 이를 해석하며 모델 파일을 패키지 설치 폴더로 복사하거나 다른 경로를 탐색해 대신 로드하지 않는다.

1. 이 계약과 실제 생산자·소비자 연결 확정
2. 충돌하는 정령별 모델·LoRA 생성 경로 제거
3. 기존 검증 데이터의 정령 구분을 단일 학습 입력에 연결
4. 실제 RTX 3070 학습과 최종 가중치 저장
5. 같은 모델에서 정령 ID를 바꿔 실제 응답 확인
6. PC 배포 경로 완성
7. Android 보강은 최후 순위

독립 테스트 작성·임시 실험 반복·외부 소스 논의가 실제 코드와 학습보다 앞서지 않는다. 관련 lint·실행 오류의 원인을 고치며 진단을 숨기지 않는다.

## 9. 완료 판단

최종 모델 하나에서 플랫폼 지정 정령의 1인칭, 개별 말투·성격·세계관·개인사 소유, 세 언어 응답, 일반 지식과 연속 대화의 정체성, 다른 정령 이름 등장 시 선택 정령 유지, 세션 간 혼입 방지, PC의 실제 최종 가중치 소비를 확인한다.

loss나 이름 검사만으로 품질을 완성했다고 선언하지 않는다. 관찰한 실패는 수정 작업이며 미확인 품질을 완벽하다고 보장하지 않는다. 중간 설명으로 원래 작업을 끝내지 않는다.
